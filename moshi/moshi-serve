#!/bin/bash
set -e

HF_REPO="${1:-kyutai/moshiko-pytorch-bf16}"

echo "=== Moshi Server ==="
echo "Model:    $HF_REPO"
echo "Port:     8998"
echo "====================="

exec python -m moshi.server \
    --host 0.0.0.0 \
    --port 8998 \
    --hf-repo "$HF_REPO"
